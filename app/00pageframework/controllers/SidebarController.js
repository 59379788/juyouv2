module.exports = function($scope, $rootScope){

	// Layout.initSidebar();

	// $scope.currentmenu = $rootScope.currentmenu;

	// console.log($scope.currentsys);

	// $scope.choosemenu = function(menu)
	// {
	// 	$rootScope.currentmenu = $scope.currentmenu = menu;
	// };
};